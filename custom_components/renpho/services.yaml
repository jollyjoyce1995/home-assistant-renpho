import_history:
  name: Import Historical Data
  description: Fetches complete historical measurement data from the Renpho cloud and imports it into Home Assistant Long-Term Statistics.
  fields:
    entry_id:
      name: Config Entry ID
      description: Optional ID of the specific Renpho config entry to import history for. Leave empty to import for all configured accounts.
      required: false
      selector:
        text:
